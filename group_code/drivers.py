class Drivers:
    def __init__(self):
        self.driver_list = all_drivers()
        self.busy_drivers = unavailable()
        self.available_drivers = available_drivers()

    def all_drivers(self, drivers):
        drivers = []
        for i in employee_drivers:
            drivers.append(employees_list)
            return drivers

    def available(self):
        for drivers in self.employee_list: #looking at list of employees that are drivers
            if driver == "on a delivery":
                return driver_unavailable
            elif driver == "not working":
                return driver_unavailable
            else:
                return driver_available

